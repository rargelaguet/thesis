\begin{tikzpicture}

% Define nodes
\node[obs] (Y) {$\bfy_{n}$};
\node[latent, above=of Y] (local) {$\bfz_{n}$};
\node[latent, xshift=1.5cm] (global) {$\bbeta$};
\node[latent, double, double distance=1pt, right=of global] (global_params) {$\balpha_{\beta}$};
\node[latent, double, double distance=1pt, right=of local] (local_params) {$\balpha_{z}$};

% Connect the nodes
\edge {global,local} {Y};
\edge[] {global_params} {global};
\edge[] {local_params} {local};
\edge[dashed] {global} {local};

% Plates
\plate[] {plateN} {(local)(Y)} {$N$};

\end{tikzpicture}